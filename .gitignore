# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
.idea/
.DS_Store
main

# justgo
tmp
vendor
# if you un-ignore vendor, uningore this, as well
Gopkg.lock 
vendor.orig
